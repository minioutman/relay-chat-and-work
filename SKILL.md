---
name: relay-work
version: 1.0.0
description: >
  接力完成任务的长任务管理技能。把一个大活/长任务拆成多个有编号的阶段,
  分阶段一路推进,每阶段进度持久化到文件,支持跨会话续接(下次说句话接着干)。
  触发词: "接力干活"、"接力"、"分阶段做"、"拆成步骤一步步做"、
  "先做xx再做xx"、"做一半下次继续"、"接着干"、"继续上次"、"续上"、
  "long task"、"multi-step"、"resume"、"continue where I left off"。
  典型场景: 写多章节文档/书、批量处理数据、多步骤生成报告、跨多次会话的项目。
---

# relay-work: 接力完成任务

把长任务拆成阶段,分步推进,进度存文件,跨会话可续接。

## 何时使用

当用户给出的是一个**一次性做不完 / 需要分多阶段 / 想逐段推进**的长任务时。

三板:
1. 拆阶段
2. 阶段推进 + 进度落盘
3. 支持续接

## 工作流程

### 第1步: 接任务,先拆阶段

先把任务写成一段"目标"话,然后拆成 2~N 个阶段,每个阶段有明确的**交付物**。
尽量让阶段之间"上一段产出 = 下一段输入",这样自然接力。

### 第2步: 初始化进度文件

在 `/var/minis/workspace/relay-work/` 建 `<task-slug>.md`,
按 `references/protocol.md` 的结构初始化:目标、计划清单、续接点。

### 第3步: 逐阶段推进

- 每完成一个阶段的交付物,就更新进度文件(勾选、补阶段进度、重写续接点)。
- 阶段之间默认自动推进;遇到需要用户拍板的,停下并置「等待确认」。
- 阶段交付物有明确校验标准时(如脚本跑通、文件生成、数量达标),先校验再进下一段。

### 第4步: 完成或归档

全部阶段完成 → 在进度文件标「已完成」,向用户交付最终结果,可询问是否需要把进度文件删除或留着。

## 跨会话续接的约定

- 用户说「继续 / 接着干 / 绨上上次」且能对应到某个进度文件时:读文件 → 看续接点 → 从那儿继续,不重复已完成的。
- 优先确认续接点,而不是重新问用户从哪开始。

## 与"手把手人机配合"的区别

- 如果用户是想**每一步来检查再走下一步**(人机来回、单步确认),那是另一回事——那种情况听用户指令走,不强自动推进。
- 本技能默认是"自动接力推进 + 关键点停下确认"。

## 参考

详细的状态文件规范见 `references/protocol.md`。
